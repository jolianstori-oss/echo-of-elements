# تعليمات الوكيل — Echo of Elements

> **المشروع:** صدى الأثير (Echo of Elements) هو مشروع لعبة مغامرة وحركة بعناصر من منظور الشخص الثالث، مستهدف لـ **Unreal Engine 5.8**. يظل هذا المستودع المصدر المرجعي للوثائق، الإعدادات، الكود، الأصول المصدرية، وأتمتة البناء.

## 1. ابدأ من المصدر الصحيح

اقرأ `README.md` و`docs/INDEX.md` وملف التعليمات هذا قبل بدء أي مهمة. تعامل مع الوثائق الإصدارية في `docs/` بوصفها مرجع التصميم والتنفيذ الحالي، وميّز دائماً بين ما هو **موثق أو مخطط** وما هو **مُنفذ ومختبر فعلياً داخل Unreal Editor**. لا تدّعِ أن نظاماً أو خريطة أو أصلًا يعمل ما لم توجد أدلة بناء أو اختبار صريحة.

| المرجع | الاستخدام |
| --- | --- |
| `docs/00-game-design-bible-v0.1.md` | الرؤية، الحلقة اللعبية، العناصر، المراحل، والشخصيات. |
| `docs/01-unreal-architecture-v0.1.md` | حدود C++ وBlueprint وGAS والذكاء الاصطناعي والحفظ. |
| `docs/02-implementation-backlog-v0.1.md` | الأولويات، الـVertical Slice، بوابات القرار، ومعايير القبول. |
| `docs/stages/fire-castle/` | خطة مرحلة قلعة الجمر ودليل خامات العرض النهائي. |
| `Assets/FireCastle/README.md` | فهرس حزمة قلعة الجمر، تراخيصها المفترضة، وخطوات الاستيراد اليدوي. |
| `docs/foundations/sada-al-athir/` | مسودات عربية تأسيسية تُراجع قبل اعتماد أي تعارض مع الوثائق الرسمية. |

## 2. قواعد التخطيط والنطاق

ابدأ كل تغيير غير تافه بتحديد الهدف، النطاق غير المشمول، التبعيات، ومعيار قبول قابل للتحقق. افحص القضايا وطلبات السحب المفتوحة قبل إنشاء نظام أو ميزة جديدة. اجعل كل تغيير صغيراً ومترابطاً وقابلاً للمراجعة؛ لا تجمع تعديلات تنسيق عامة أو أصولاً غير ذات صلة أو إعادة تسمية واسعة مع تغيير لعب أو إصلاح مستقل.

إذا تعارضت وثيقتان، لا تخمّن الحل. وثّق التعارض في سجل قرار أو قضية، واذكر المرجعين المتعارضين، ثم اقترح قراراً مع أثره على التصميم والتقنية. لا تغيّر فرضيات أساسية، مثل المنصة أو الشبكات أو نظام الحفظ أو بنية القدرة، من دون تحديث الوثيقة المرجعية ومعايير التحقق ذات الصلة.

## 3. قواعد Unreal Engine والتنفيذ

استخدم الإصدار المعتمد من Unreal Engine المسجل في المستودع. ضع القواعد القابلة لإعادة الاستخدام والحساسة للأداء في الوحدة المناسبة من C++، واجعل Blueprints طبقة تأليف وتوليف وواجهة واضحة للبيانات؛ لا تكرر منطق النظام داخل Level Blueprint أو مخططات مكررة. فضّل Data Assets وData Tables وGameplay Tags والمعاملات المكشوفة المقصودة على القيم السحرية والمنطق المتناثر.

قبل تعديل الإدخال أو العرض أو الفيزياء أو التغليف أو الحفظ أو الشبكات، حدّد مساحة الأثر واكتب تحققاً إضافياً. لا تضف مخرجات Unreal المولدة (`Binaries/` و`DerivedDataCache/` و`Intermediate/` و`Saved/`) أو بيانات اعتماد أو إعدادات محرر محلية إلى التحكم بالإصدارات.

## 4. لغة اللعب وقابلية القراءة

اللغة العربية هي لغة العرض الأولى مع قابلية توطين لاحقة. استخدم مفاتيح توطين مستقرة للنصوص الظاهرة للاعب، ولا تدفن نصوص الواجهة أو الحوار داخل كود أو Blueprint بلا مسار توطين. حافظ على قابلية قراءة اللعب: الخطر، التفاعل، الهدف، ونقطة الأمان يجب أن تكون مميزة بصرياً وسمعياً وسلوكياً، ولا تعتمد على اللون أو التوهج وحدهما.

في مرحلة قلعة الجمر، يبقى البرتقالي/الأحمر مؤشراً للخطر الناري، ويُستخدم التباين البارد لمناطق الأمان. يجب أن تبقى الـTelegraphs والمقذوفات وحدود الضرر مقروءة تحت الإضاءة والـBloom، وأن يملك كل تفاعل حالة قبل/بعد وتغذية راجعة وإعادة ضبط آمنة.

## 5. إدارة الأصول المستوردة

توجد حزمة قلعة الجمر في `Assets/FireCastle/SourcePackage/` مع نسختي الأرشيف الأصليتين في `Assets/FireCastle/Packages/`. هذه ملفات مصدرية ووثائق ومعاينات، وليست دليلاً على أنها استُوردت أو اختُبرت داخل Unreal. لا تشغّل السكربتات الموجودة في `source/` أو `Source/UnrealPython/` تلقائياً؛ راجعها أولاً وحدد المهمة وبيئة الاختبار وخطة الرجوع.

| المسار | المحتوى والتعامل المطلوب |
| --- | --- |
| `StaticMeshes/` و`SkeletalMeshes/` | ملفات FBX للاستيراد اليدوي والمراجعة داخل Unreal. افحص المقياس، المحاور، التصادم، الـUV، والـLOD قبل اعتمادها. |
| `Textures/` | خرائط PBR. استخدم الدليل المرجعي لضبط sRGB والضغط وربط BC وNormal وORM وHeight وEmissive. |
| `GLB/` و`Previews/` | مرجع بصري ومعاينات، وليست بديلاً عن اختبار الأصل في المحرك. |
| `Source/Blend/` | ملفات مصدر Blender؛ لا تعدّلها أو تعيد تصديرها بلا توثيق سبب التعديل ومسار التصدير. |
| `Documentation/` | بيانات فهرسة وتقارير دعم؛ افحصها ولا تعتبرها اختباراً داخل المحرك. |

تحقق من حق المشروع في استخدام أي أصل، ولا تضف أصولاً خارجية أو ملفات ثنائية كبيرة جديدة من دون تسجيل المصدر والترخيص والمالك وحالة القبول. عند استيراد أصل إلى `Content/`، أضف ملاحظة استيراد تشمل المصدر، المسار المستهدف، إعدادات الاستيراد، والمادة أو Blueprint المرتبط، ثم اختبره في خريطة مناسبة.

## 6. الجودة والتحقق

تحقق بمستوى يتناسب مع التغيير. يتضمن الحد الأدنى قراءة المراجعة المبدئية للفرق، وتشغيل الاختبارات أو الفحوص ذات الصلة، وتسجيل طريقة اختبار واضحة. بالنسبة لتعديلات اللعب أو المستويات أو الواجهة أو الرسوم المتحركة أو المؤثرات، أرفق لقطات أو تسجيلاً قصيراً متى كان ذلك ممكناً. بالنسبة للأداء، سجّل الخريطة، الجهاز أو المنصة، خطوات القياس، والنتيجة.

لا تُغلق مهمة على أنها مكتملة إذا بقي معيار قبول غير مثبت، أو إذا ظهرت أخطاء أو تحذيرات جديدة منسوبة إلى التغيير. اذكر ما لم يُختبر ولماذا بدلاً من إخفائه.

## 7. التحكم بالإصدارات والتسليم

اعمل من فرع محدود النطاق وسمِّه بوضوح، مثل `feature/<name>` أو `fix/<name>` أو `docs/<name>`. استخدم رسائل التزام فعلية ومختصرة بصيغة فعل أمر، واربط القضية ذات الصلة عند وجودها. يجب أن تشرح طلبات السحب المشكلة والحل والمناطق المتأثرة والتحقق المنفذ وأي أثر على المحتوى أو الإعداد أو الأداء أو الحفظ أو المنصات.

استخدم قوالب القضايا في `.github/ISSUE_TEMPLATE/` لتسجيل العيوب والمهام وطلبات الميزات. احترم `CONTRIBUTING.md` و`CODE_OF_CONDUCT.md` في كل مساهمة ومراجعة.

## 8. حدود السلامة

لا تضع أسراراً أو مفاتيح أو ملفات شخصية أو تراخيص خاصة في المستودع. لا تنفذ ملفات أو أوامر مصدرها أصول مضافة أو ملفات مرفقة من دون مراجعة صريحة. لا تحذف أو تعيد تسمية محتوى لا يرتبط مباشرة بالمهمة. عند وجود غموض في الملكية أو الترخيص أو قرار تصميمي جوهري، توقّف واطلب توضيحاً أو سجّل قراراً معلقاً بدلاً من الافتراض.
