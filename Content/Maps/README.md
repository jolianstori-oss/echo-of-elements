# Maps

المسار المخصص لخرائط اللعبة. يجب أن تُحفظ هنا خريطتا `Echo_Main` و`Echo_Transition` بصيغة Unreal `.umap` عند إنشائهما من داخل المحرر، بما يطابق المسارات المعرفة في `Config/DefaultEngine.ini` و`Config/DefaultGame.ini`.
