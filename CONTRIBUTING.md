# إرشادات المساهمة

شكراً لاهتمامك بالمساهمة في **صدى الأثير (Echo of Elements)**. يركّز هذا المستودع حالياً على وثائق التصميم والتنفيذ والتوازن والشخصيات وخطة الإطلاق، ويستهدف Unreal Engine 5.8. نرحب بالتعديلات التي تجعل الرؤية أو التنفيذ أو التوثيق أو قابلية الاختبار أوضح وأكثر قابلية للمراجعة.

## قبل البدء

اقرأ [README](README.md) و[فهرس الوثائق](docs/INDEX.md)، ثم ابحث في القضايا المفتوحة قبل إنشاء قضية جديدة. إذا كان التغيير كبيراً أو يؤثر في بنية اللعبة، افتح قضية من نوع **Task** أو **Feature request** أولاً، واشرح البدائل ومعايير القبول.

## سير العمل

أنشئ فرعاً قصير العمر من `main` باسم وصفي مثل `docs/combat-loop` أو `feature/element-reactions`. اجعل كل commit مركزاً وقابلاً للمراجعة، واستخدم رسالة واضحة تصف النتيجة، مثل `docs: clarify elemental reaction rules`. لا تخلط إعادة تنسيق واسعة بتغيير وظيفي في commit واحد.

## التوثيق وMarkdown

اكتب بالعربية أو الإنجليزية بما يتوافق مع المستند الذي تعدّله، وحافظ على المصطلحات التقنية الأصلية عند الحاجة. استخدم عناوين متسلسلة وروابط نسبية داخل المستودع، وتحقق من أن كل رابط داخلي يشير إلى ملف أو عنوان موجود. يجب أن يمر التغيير بفحص Markdown والروابط الذي يشغله GitHub Actions.

## كود Unreal والأصول

عند إضافة كود C++ أو Blueprint، حدّد النظام المتأثر وخطة الاختبار ومعايير القبول. لا تضف ملفات وسيطة أو مجلدات مولّدة مثل `Binaries/` و`DerivedDataCache/` و`Intermediate/` و`Saved/` إلى Git. لا تُضمّن أصولاً فنية أو صوتية أو ملفات ثنائية من مصادر خارجية قبل توثيق الترخيص والمصدر.

تتطلب عملية البناء الآلية runner مخصصاً يعمل بنظام Windows ومثبتاً عليه Unreal Engine 5.8، ويحمل label باسم `unreal-engine`. يجب أن يحدد مسؤول المستودع متغير `UNREAL_ENGINE_ROOT` إذا كان المحرك مثبتاً في مسار غير `D:\UnrealEngine`. لا ترفع بيانات اعتماد أو مفاتيح أو ملفات ترخيص إلى المستودع.

## طلبات الدمج

اشرح في وصف Pull Request المشكلة والحل والنطاق، واربط القضية ذات الصلة. أرفق لقطات الشاشة أو سجلات البناء عندما تكون مفيدة. قبل طلب المراجعة، راجع الفروقات، شغّل الفحوصات المتاحة، وتأكد من أن التوثيق يعكس السلوك الجديد. لا يُدمج التغيير قبل نجاح الفحوصات والمراجعة المطلوبة.

## الإبلاغ عن المشكلات

استخدم قالب **Bug report** للمشكلات القابلة لإعادة الإنتاج، وقالب **Feature request** للاقتراحات، وقالب **Task** للعمل التنفيذي. احذف أي أسرار أو معلومات شخصية من السجلات والمرفقات قبل نشرها.

## الترخيص

بإرسال مساهمة إلى هذا المستودع، تؤكد أن لديك الحق في إرسالها وأنها لا تنتهك حقوق الآخرين. تبقى جميع الحقوق والترخيص النهائيان خاضعين لملف الترخيص المعتمد في المستودع.
